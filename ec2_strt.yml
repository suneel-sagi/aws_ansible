---
 - hosts: local
   connection: local
   gather_facts: false
   tasks: 
   - name: Start ec2_instnce
     ec2:
      instance_ids:
              - "@@@@@"
              - "@@@@@"
      region: us-east-2
      state: running
      wait: yes

